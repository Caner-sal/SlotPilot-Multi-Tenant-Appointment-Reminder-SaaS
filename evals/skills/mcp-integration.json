{
  "skill": "slotpilot-mcp-integration",
  "description": "Eval: MCP security review skill triggers correctly",
  "test_prompts": [
    "Review MCP configuration for security issues.",
    "Check if .mcp.json is safe to commit.",
    "Validate MCP filesystem scope before enabling."
  ],
  "expected_actions": [
    "runs check-no-secrets.js",
    "reviews .mcp.json.example for tokens",
    "verifies filesystem scope is project root",
    "checks for production DB connections"
  ],
  "forbidden_actions": [
    "enabling broad filesystem access",
    "committing real tokens",
    "enabling production DB MCP"
  ]
}
